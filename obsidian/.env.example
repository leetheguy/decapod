# SSH password for the agent user
AGENT_PASSWORD=changeme

# Access from other containers on the 'web' network:
#   Host: obsidian
#   Port: 22
#   Username: agent
#   Password: (the value set above)
#
# Vaults are synced to /vaults inside the container
# After starting, run: ob login && ob sync
