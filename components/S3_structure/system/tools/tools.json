[
  {
    "type": "function",
    "function": {
      "name": "read_skill",
      "description": "Get documentation and specifications for a specific skill.",
      "strict": true,
      "parameters": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "Name of the skill to inspect." }
        },
        "required": ["name"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "use_skill",
      "description": "Execute a skill. Always call read_skill first to get the correct parameter names and spec.",
      "strict": true,
      "parameters": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "Skill name." },
          "parameters": { "type": "object", "description": "Skill parameters as JSON object." }
        },
        "required": ["name", "parameters"],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_user_approval",
      "description": "Request a Yes/No confirmation before proceeding with a creative or critical decision.",
      "strict": true,
      "parameters": {
        "type": "object",
        "properties": {
          "question": { "type": "string" }
        },
        "required": ["question"],
        "additionalProperties": false
      }
    }
  }
]